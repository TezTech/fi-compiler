# Primitive imput
storage nat test;
entry TestInputSimple(
	bool boolLabel
	, nat natLabel
	, int intLabel
	, string stringLabel
	, mutez mutezLabel
	, address addressLabel
	, key keyLabel
	, pkh pkhLabel
	, signature signatureLabel
	, bytes bytesLabel
) {
	storage.test = input.natLabel;
}

# Complex input - not yet implemented.
# storage map[string => nat] map;
# entry TestInputComplex(
# 	map[string => nat] mapLabel
# 	, string[] listLabel
# 	, set[nat] setLabel
# ) {
# 	storage.map = input.mapLabel;
# }